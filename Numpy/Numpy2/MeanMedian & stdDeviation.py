import numpy as np

numbers = np.array([10,20,30,40,50])

print(np.mean(numbers)) # Avarage Value
print(np.median(numbers)) # Middle value after Sorting

# Variance
print(np.std(numbers)) # tiap angka di dikurangi mean lalu di kuadrtkan
